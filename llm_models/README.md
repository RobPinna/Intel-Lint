# Local Model Cache Placeholder

This directory is intentionally tracked only as a placeholder for local Ollama model data.

- Do not commit model blobs or manifests here.
- Model weights are downloaded locally by the user runtime.
- If you use `start-dev.cmd ollama ...`, Ollama can use this directory as local cache.
